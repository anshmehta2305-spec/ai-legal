{
  "name": "ai-legal-clustering-root",
  "version": "1.0.0",
  "description": "Root forwarding configuration for AI Legal Text Clustering",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
